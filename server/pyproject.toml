[project]
name = "flaskr"
version = "1.0.0"
description = "The basic blog app built in the Flask tutorial."
dependencies = [
    "flask",
    "numpy",
    "pandas",
    "scikit-learn",
    "matplotlib",
    "seaborn",
]